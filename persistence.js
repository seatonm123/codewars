const n = 39;
const n2 = 4;

function persistence(num){
  let s = num.toString().split('');
  let counter = 0;
  
  while(counter === 0 || s.length > 1) {

  }
}

console.log(persistence(n));
console.log(persistence(n2));
